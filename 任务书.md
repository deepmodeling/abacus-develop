# 机器学习分子动力学并行优化任务计划书

## 一、项目目标

本项目拟围绕 ABACUS 分子动力学模块中的机器学习势函数计算进行并行优化。我们计划优先完成两个核心任务：

第一，完成**题目2：OpenMP 多线程加速势函数计算**。该部分将从 DPMD 和 NEP 势函数的计算流程入手，分析其中可并行化的循环结构、数据依赖关系和主要计算热点，并尝试使用 OpenMP 对势函数计算部分进行多线程加速。

第二，完成**题目6：NEP 势函数的并行优化**。在完成通用 OpenMP 并行化分析后，我们将进一步聚焦 NEP 势函数，重点优化原子密度计算、多元素系统处理以及内存访问模式，并通过串行与并行结果对比验证正确性。

若上述任务按期完成，我们将进一步探索**开放题目2：分子动力学与第一性原理计算的无缝集成**，尝试设计 AIMD 与机器学习势函数结合的混合模拟框架，重点放在方案设计、接口分析和初步代码框架，而不强求完整实现。

---

## 二、任务分工与技术路线

## 1. 前期调研与代码理解

在 4.27 至 5.13 期间，小组成员首先完成题目阅读、项目背景调研和代码结构熟悉。重点阅读 ABACUS 分子动力学模块中的主循环、势函数接口以及机器学习势函数实现文件，包括：

* `source/source_md/md.cpp`
* `source/source_md/md.h`
* `source/source_md/potential/potential.h`
* `source/source_md/potential/ml/dpmd.cpp`
* `source/source_md/potential/ml/nep.cpp`

该阶段的主要目标是明确势函数计算在分子动力学模拟中的位置，理解 `calculate_energy_and_force` 等核心接口的调用逻辑，并初步判断哪些循环或计算模块适合 OpenMP 并行化。ABACUS 的 MD 主类中包含积分器、温控器、压控器、势函数、原子位置、速度、受力和 MPI 通信信息，因此势函数并行优化需要尽量保持与现有接口兼容。

---

## 2. 题目2：OpenMP 多线程加速势函数计算

### 目标

完成势函数计算部分的 OpenMP 并行化初步实现，重点关注循环级并行、线程安全和串并行结果一致性。

### 主要工作

首先，我们将对 DPMD 和 NEP 势函数代码进行热点分析，识别主要耗时函数和可并行化循环。预计重点关注原子遍历、邻居原子遍历、能量累加、力累加等部分。

其次，我们将在不存在明显数据竞争的循环中加入 OpenMP 并行指令，例如：

```cpp
#pragma omp parallel for
for (int i = 0; i < natom; ++i) {
    // compute local contribution
}
```

对于涉及总能量累加的部分，将使用 `reduction`；对于涉及 force 数组写入的部分，将重点检查是否存在多个线程同时写入同一原子受力的情况，必要时采用线程私有缓存、局部数组归并或原子操作。

最后，我们将设计基本测试，比较不同线程数下的计算结果和运行时间。测试内容包括：

* 串行版本与 OpenMP 版本的能量一致性；
* 串行版本与 OpenMP 版本的 force 数组误差；
* `OMP_NUM_THREADS=1,2,4,8` 下的运行时间；
* 加速比和并行效率。

### 阶段产出

到 5.20 前，完成背景调研材料和详细算法文档，说明 OpenMP 并行化位置、数据依赖分析、预期风险和测试方案。

---

## 3. 题目6：NEP 势函数的并行优化

### 目标

在题目2的基础上，进一步针对 NEP 势函数进行更深入优化，使其不仅“能并行”，而且在内存访问、循环结构和多元素处理方面具有更好的性能表现。

### 主要工作

第一，分析 NEP 势函数的计算流程。重点关注原子密度计算、邻居信息访问、多元素参数索引和神经网络前向计算等部分，判断哪些模块是计算热点。

第二，对 NEP 中的主要循环进行 OpenMP 优化。优先考虑以原子为单位进行并行，即每个线程负责一部分原子的局部能量、局部描述符或局部密度计算。

第三，优化内存访问模式。若原始代码中存在频繁的非连续访问、重复计算或临时数组反复分配，我们将尝试进行以下改进：

* 将循环中反复使用的变量局部化；
* 减少临时对象的动态分配；
* 尽量保持数组连续访问；
* 将线程私有变量与共享变量区分清楚；
* 对多元素系统中的参数索引进行预处理或缓存。

第四，完成正确性验证与性能测试。题目6要求验证不同并行配置下的结果一致性，并测试多元素系统的计算正确性，因此我们将准备至少两类测试体系：一个单元素体系用于基础验证，一个多元素体系用于 NEP 多元素处理测试。

### 阶段产出

到 5.27 前，完成代码分析结论和初步测试结果；到 6.3 前，完成第一轮优化方案和结果；到 6.10 前，完成进一步优化和更完整的性能测试。

---

## 4. 开放题目2：分子动力学与第一性原理计算的无缝集成

### 目标

若题目2和题目6进展顺利，我们将把开放题目2作为拓展方向，完成初步方案设计和可能的代码框架。

开放题目2要求设计 AIMD 与机器学习势函数结合的混合模拟方案，实现不同势函数之间的平滑切换，并优化第一性原理计算与分子动力学之间的协同。

### 拟探索内容

我们计划将该部分定位为“框架设计 + 初步接口实现”，不作为主线任务。可能的设计包括：

```cpp
class HybridMD {
private:
    FirstPrinciplesEngine* fp_engine;
    MLPotential* ml_potential;
    SwitchingStrategy* switch_strategy;

public:
    void run();
};
```


该部分最终产出可能是一个最小可运行的实现。

---

# 三、时间安排

| 时间节点   | 对应周次 | 计划任务                               | 预期产出                     |
| ------ | ---: | ---------------------------------- | ------------------------ |
| 4.27 前 | 第9周前 | 阅读题目说明，理解大作业要求，完成组队和选题             | 明确主线题目：题目2、题目6；开放题目2作为拓展 |
| 5.13   | 第11周 | 完成项目调研，写出项目计划书                     | 提交任务计划书；完成代码结构初步阅读       |
| 5.20   | 第12周 | 分析 DPMD/NEP 势函数计算流程，定位 OpenMP 并行机会 | 提交背景调研资料和详细算法文档          |
| 5.27   | 第13周 | 完成题目2的初步 OpenMP 实现，并开始 NEP 代码分析    | 提交代码分析结论和初步测试结果          |
| 6.3    | 第14周 | 完成 NEP 势函数第一轮并行优化                  | 提交初步优化方案、加速比结果和正确性验证     |
| 6.10   | 第15周 | 深入优化 NEP 计算流程，完善测试和性能对比            | 提交深入优化方案、更多线程数测试和多元素体系验证 |
| 6.17   | 第16周 | 整理最终结果，完成总结报告；若有余力补充开放题目2设计        | 提交最终优化效果、实验图表和总结报告       |

---

# 四、预期测试方案

我们计划从正确性和性能两个方面进行测试。

## 1. 正确性测试

正确性测试主要比较串行版本与并行版本的输出是否一致，包括：

* 总能量误差；
* 每个原子的 force 误差；
* 不同线程数下结果是否稳定；
* 单元素体系与多元素体系是否均能正常运行；
* 是否出现数据竞争、NaN 或程序崩溃。

对于浮点数结果，不要求完全逐位一致，但应控制在合理误差范围内，例如：

```text
|E_parallel - E_serial| < 1e-8
max(|F_parallel - F_serial|) < 1e-8
```

具体阈值可根据实际代码和测试体系适当调整。

## 2. 性能测试

性能测试计划设置不同线程数：

```bash
export OMP_NUM_THREADS=1
export OMP_NUM_THREADS=2
export OMP_NUM_THREADS=4
export OMP_NUM_THREADS=8
```

记录每种设置下的势函数计算时间、总 MD 步耗时和加速比。测试脚本可参考作业说明中给出的 benchmark 脚本，作业中也建议通过不同 `nproc` 和 `nthread` 组合评估并行效率。

---